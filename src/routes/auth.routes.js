// Import d'Express en utilisant la syntaxe ES6 modules (cohérent avec le reste du projet)
import express from 'express';
// Création d'un routeur Express pour gérer les routes d'authentification
const router = express.Router();

// Export du routeur par défaut pour utilisation dans app.js
export default router;